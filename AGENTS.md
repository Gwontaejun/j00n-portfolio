# J00N Portfolio 작업 규칙

이 문서는 이 저장소에서 작업하는 사람과 AI가 항상 지켜야 하는 기준이다. 작업을 시작할 때 `README.md`와 `docs/PROJECT_STATE.md`도 함께 읽는다.

## 소통과 검증

- 사용자에게는 기본적으로 한국어로 답하고 UI 문구도 한국어를 사용한다.
- 사용자가 영어 표기를 명시한 프로젝트명·부제는 임의로 번역하지 않는다.
- 시각적 수정을 완료했다고 말하기 전에 가능한 경우 실제 화면으로 검증한다.
- 화면을 직접 검증하지 못했다면 그 사실을 명확히 밝힌다.
- 기존 카메라, 모델, `Html`의 위치·회전·크기 값은 시각적으로 조율된 값이다. 요청 범위 밖에서 크게 바꾸지 않는다.
- 사용자 변경을 덮어쓰거나 관련 없는 파일을 정리하지 않는다.

## 기술 및 구조

- Next.js App Router, TypeScript strict mode, Tailwind CSS, React Three Fiber, drei, Framer Motion을 사용한다.
- 개발과 빌드는 공식 Next.js CLI인 `next dev`, `next build`, `next start` npm 스크립트를 사용한다.
- 경로 별칭 `@/*`는 `src/*`를 가리킨다.
- 프로젝트 데이터는 `src/data/projects.ts`, 타입은 `src/types/project.ts`에서 관리한다.
- 기능별 코드는 `src/features`, 여러 기능이 공유하는 코드는 `src/shared`에 둔다.
- GLB 모델은 `public/3d-models`의 기존 파일을 우선 사용한다. 승인 없이 조잡한 기본 도형으로 대체하지 않는다.
- 생성 파일인 `*.tsbuildinfo`, `.next`, 이전 런타임 잔여물인 `.vinext`, `out`은 커밋하지 않는다.

## 핵심 UX 규칙

- 기본 장면은 비 오는 창문이 있는 꾸며진 개발자 작업 공간이다.
- 최초 카메라는 잠금 상태다. 책상 화면에서만 우측 상단 버튼으로 카메라 이동을 켤 수 있다.
- 모니터 클릭 시 카메라가 모니터 앞으로 이동한다.
- 휴대폰 클릭 시 카메라는 움직이지 않고, 휴대폰 모델이 현재 시점 앞으로 이동해 정면을 향한다.
- 모니터, 휴대폰, 프로필 상세 중 하나만 활성화할 수 있다.
- 상세 화면이 활성화된 동안 배경의 다른 3D 오브젝트와 HTML UI는 클릭되지 않아야 한다.
- 배경 클릭으로 상세 화면을 닫지 않는다. ESC, 좌측 상단 뒤로가기 아이콘 또는 명시적인 닫기 동작만 사용한다.
- 모니터와 휴대폰의 복귀 안내 문구와 스타일은 통일한다: `ESC 키 또는 좌측 상단 화살표를 누르면 책상으로 돌아갑니다.`
- 모니터와 휴대폰 화면 HTML은 기기 뒤쪽에서 보이지 않아야 한다.
- 클릭 가능한 3D 대상은 키보드 포커스, Enter/Space 동작, 적절한 `aria-label`을 유지한다.

## 디자인 기준

- 작고 흐린 글씨, 과도한 여백, 불필요한 카드 중첩을 피한다.
- 밝고 읽기 쉬운 화면을 유지하되 방 조명과 동떨어질 정도로 새하얗게 만들지 않는다.
- 기능을 구분하기 위한 색은 절제해서 사용하고, GitHub 같은 브랜드에 근거 없는 주황색을 적용하지 않는다.
- 모바일 앱 화면은 Android 문법을 따른다. 하단 탐색은 왼쪽 최근 앱, 가운데 홈, 오른쪽 뒤로가기 순서다.
- 프로젝트 상세는 정보 위계가 분명해야 하며 링크, 기술 스택, 소개, 주요 기능의 순서를 함부로 뒤섞지 않는다.

## 작업 완료 전

```bash
npm run typecheck
npm run lint
```

큰 변경은 가능하면 `npm run build`도 실행한다. 구현 상태나 다음 작업이 달라졌다면 `docs/PROJECT_STATE.md`를 함께 갱신한다.
